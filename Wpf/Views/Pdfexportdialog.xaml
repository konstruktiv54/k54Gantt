<Window x:Class="Wpf.Views.PdfExportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Экспорт в PDF"
        Width="420"
        Height="580"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#FAFAFA">

    <Window.Resources>
        <Style x:Key="TitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="#212121"/>
        </Style>
        <Style x:Key="SubtitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#757575"/>
        </Style>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#424242"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
        <Style x:Key="SectionStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="#1976D2"/>
            <Setter Property="Margin" Value="0,12,0,8"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderBrush" Value="#BDBDBD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="8,6"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="0,4"/>
        </Style>
        <Style TargetType="RadioButton">
            <Setter Property="Margin" Value="0,4"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <StackPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="Экспорт диаграммы Ганта в PDF" 
                       Style="{StaticResource TitleStyle}"/>
            <TextBlock Text="Настройте параметры экспорта" 
                       Style="{StaticResource SubtitleStyle}"
                       Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Настройки -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                
                <!-- Название проекта -->
                <TextBlock Text="Название проекта" Style="{StaticResource LabelStyle}"/>
                <TextBox x:Name="ProjectNameTextBox" Margin="0,0,0,8"/>

                <!-- ═══════════════════════════════════════════════════════════════ -->
                <!-- СЕКЦИЯ: Режим размещения -->
                <!-- ═══════════════════════════════════════════════════════════════ -->
                <TextBlock Text="📐 Режим размещения" Style="{StaticResource SectionStyle}"/>
                
                <RadioButton x:Name="FitToPageRadioButton" 
                             GroupName="PageMode"
                             Content="Вся диаграмма на одной странице (рекомендуется)"
                             IsChecked="True"/>
                
                <RadioButton x:Name="MultiPageRadioButton" 
                             GroupName="PageMode"
                             Content="Разбить на несколько страниц"/>
                
                <TextBlock Text="В режиме 'одна страница' размер подбирается автоматически. Формат и ориентация игнорируются." 
                           FontSize="10" 
                           Foreground="#9E9E9E" 
                           Margin="16,0,0,8" 
                           TextWrapping="Wrap"/>

                <!-- ═══════════════════════════════════════════════════════════════ -->
                <!-- СЕКЦИЯ: Страница (только для многостраничного режима) -->
                <!-- ═══════════════════════════════════════════════════════════════ -->
                <TextBlock Text="📄 Страница" Style="{StaticResource SectionStyle}"/>
                
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Формат -->
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Формат" Style="{StaticResource LabelStyle}"/>
                    <ComboBox x:Name="PaperFormatComboBox" Grid.Row="1" Grid.Column="0" SelectedIndex="0">
                        <ComboBoxItem Content="A4 (210 × 297 мм)" Tag="A4"/>
                        <ComboBoxItem Content="A3 (297 × 420 мм)" Tag="A3"/>
                        <ComboBoxItem Content="A2 (420 × 594 мм)" Tag="A2"/>
                        <ComboBoxItem Content="A1 (594 × 841 мм)" Tag="A1"/>
                        <ComboBoxItem Content="Letter (8.5 × 11 in)" Tag="Letter"/>
                        <ComboBoxItem Content="Legal (8.5 × 14 in)" Tag="Legal"/>
                        <ComboBoxItem Content="Tabloid (11 × 17 in)" Tag="Tabloid"/>
                    </ComboBox>
                    
                    <!-- Ориентация -->
                    <TextBlock Grid.Row="0" Grid.Column="2" Text="Ориентация" Style="{StaticResource LabelStyle}"/>
                    <ComboBox x:Name="OrientationComboBox" Grid.Row="1" Grid.Column="2" SelectedIndex="0">
                        <ComboBoxItem Content="Альбомная" Tag="Landscape"/>
                        <ComboBoxItem Content="Книжная" Tag="Portrait"/>
                    </ComboBox>
                </Grid>

                <TextBlock Text="⚠️ Формат и ориентация применяются только в многостраничном режиме." 
                           FontSize="10" 
                           Foreground="#FF6F00" 
                           Margin="0,0,0,8" 
                           TextWrapping="Wrap"/>

                <!-- ═══════════════════════════════════════════════════════════════ -->
                <!-- СЕКЦИЯ: Качество и масштаб -->
                <!-- ═══════════════════════════════════════════════════════════════ -->
                <TextBlock Text="🔍 Качество и масштаб" Style="{StaticResource SectionStyle}"/>

                <!-- Качество -->
                <TextBlock Text="Качество (DPI)" Style="{StaticResource LabelStyle}"/>
                <ComboBox x:Name="QualityComboBox" SelectedIndex="1" Margin="0,0,0,8">
                    <ComboBoxItem Content="Черновик (96 DPI) — быстро, малый размер" Tag="96"/>
                    <ComboBoxItem Content="Стандарт (150 DPI) — оптимально" Tag="150"/>
                    <ComboBoxItem Content="Высокое (200 DPI) — для печати" Tag="200"/>
                    <ComboBoxItem Content="Максимальное (300 DPI) — большой файл" Tag="300"/>
                </ComboBox>

                <!-- Масштаб (только для многостраничного) -->
                <TextBlock Style="{StaticResource LabelStyle}">
                    <Run Text="Масштаб диаграммы (для многостраничного режима)"/>
                </TextBlock>
                <Grid Margin="0,0,0,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="60"/>
                    </Grid.ColumnDefinitions>
                    <Slider x:Name="ScaleSlider" 
                            Minimum="50" Maximum="200" Value="100"
                            TickFrequency="25" IsSnapToTickEnabled="True"
                            TickPlacement="BottomRight"
                            VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1" 
                               Text="{Binding ElementName=ScaleSlider, Path=Value, StringFormat={}{0:0}%}"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Right"
                               FontWeight="Medium"/>
                </Grid>
                <TextBlock Text="100% = вписывается в ширину страницы. Меньше = мельче, больше помещается." 
                           FontSize="10" Foreground="#9E9E9E" Margin="0,0,0,8" TextWrapping="Wrap"/>

                <!-- ═══════════════════════════════════════════════════════════════ -->
                <!-- СЕКЦИЯ: Опции -->
                <!-- ═══════════════════════════════════════════════════════════════ -->
                <TextBlock Text="⚙️ Опции" Style="{StaticResource SectionStyle}"/>
                
                <CheckBox x:Name="ShowHeaderCheckBox" 
                          Content="Показывать заголовок на каждой странице"
                          IsChecked="True"/>
                
                <CheckBox x:Name="ShowDateCheckBox" 
                          Content="Показывать дату экспорта"
                          IsChecked="True"/>
                
                <CheckBox x:Name="ShowPageNumbersCheckBox" 
                          Content="Показывать номера страниц"
                          IsChecked="True"/>
                
                <CheckBox x:Name="OpenAfterExportCheckBox" 
                          Content="Открыть PDF после создания"
                          IsChecked="True"/>

            </StackPanel>
        </ScrollViewer>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            
            <Button Content="Отмена" 
                    IsCancel="True"
                    Padding="16,8"
                    Margin="0,0,8,0"
                    Width="100"/>
            
            <Button x:Name="ExportButton"
                    Content="📄 Экспорт"
                    Click="ExportButton_Click"
                    IsDefault="True"
                    Padding="16,8"
                    Width="120"
                    Background="#1976D2"
                    Foreground="White"
                    BorderThickness="0"/>
            
        </StackPanel>
    </Grid>
</Window>
