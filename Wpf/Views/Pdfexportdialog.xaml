<Window x:Class="Wpf.Views.PdfExportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Экспорт в PDF"
        Width="400"
        Height="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#FAFAFA">

    <Window.Resources>
        <!-- Локальные стили без MaterialDesign -->
        <Style x:Key="TitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="#212121"/>
        </Style>
        <Style x:Key="SubtitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#757575"/>
        </Style>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#424242"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderBrush" Value="#BDBDBD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="8,6"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="0,4"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Экспорт диаграммы Ганта в PDF" 
                       Style="{StaticResource TitleStyle}"/>
            <TextBlock Text="Настройте параметры экспорта" 
                       Style="{StaticResource SubtitleStyle}"
                       Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Настройки -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                
                <!-- Название проекта -->
                <TextBlock Text="Название проекта" Style="{StaticResource LabelStyle}"/>
                <TextBox x:Name="ProjectNameTextBox"
                         Margin="0,0,0,16"/>

                <!-- Ориентация -->
                <TextBlock Text="Ориентация страницы" Style="{StaticResource LabelStyle}"/>
                <ComboBox x:Name="OrientationComboBox"
                          SelectedIndex="0"
                          Margin="0,0,0,16">
                    <ComboBoxItem Content="Альбомная (Landscape)" Tag="Landscape"/>
                    <ComboBoxItem Content="Книжная (Portrait)" Tag="Portrait"/>
                </ComboBox>

                <!-- Качество -->
                <TextBlock Text="Качество" Style="{StaticResource LabelStyle}"/>
                <ComboBox x:Name="QualityComboBox"
                          SelectedIndex="1"
                          Margin="0,0,0,16">
                    <ComboBoxItem Content="Экранное (96 DPI)" Tag="96"/>
                    <ComboBoxItem Content="Среднее (150 DPI)" Tag="150"/>
                    <ComboBoxItem Content="Высокое (200 DPI)" Tag="200"/>
                    <ComboBoxItem Content="Печатное (300 DPI)" Tag="300"/>
                </ComboBox>

                <!-- Масштаб -->
                <TextBlock Text="Масштаб" Style="{StaticResource LabelStyle}"/>
                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="60"/>
                    </Grid.ColumnDefinitions>
                    <Slider x:Name="ScaleSlider" 
                            Minimum="50" Maximum="150" Value="100"
                            TickFrequency="10" IsSnapToTickEnabled="True"
                            VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1" 
                               Text="{Binding ElementName=ScaleSlider, Path=Value, StringFormat={}{0:0}%}"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Right"/>
                </Grid>

                <!-- Опции -->
                <TextBlock Text="Опции" Style="{StaticResource LabelStyle}" Margin="0,8,0,8"/>
                
                <CheckBox x:Name="ShowHeaderCheckBox" 
                          Content="Показывать заголовок на каждой странице"
                          IsChecked="True"/>
                
                <CheckBox x:Name="ShowDateCheckBox" 
                          Content="Показывать дату экспорта"
                          IsChecked="True"/>
                
                <CheckBox x:Name="ShowPageNumbersCheckBox" 
                          Content="Показывать номера страниц"
                          IsChecked="True"/>
                
                <CheckBox x:Name="OpenAfterExportCheckBox" 
                          Content="Открыть PDF после создания"
                          IsChecked="True"/>

            </StackPanel>
        </ScrollViewer>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            
            <Button Content="Отмена" 
                    IsCancel="True"
                    Padding="16,8"
                    Margin="0,0,8,0"
                    Width="100"/>
            
            <Button x:Name="ExportButton"
                    Content="📄 Экспорт"
                    Click="ExportButton_Click"
                    IsDefault="True"
                    Padding="16,8"
                    Width="110"
                    Background="#1976D2"
                    Foreground="White"
                    BorderThickness="0"/>
            
        </StackPanel>
    </Grid>
</Window>
